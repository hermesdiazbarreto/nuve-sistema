name: Backup Reminder

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  reminder:
    runs-on: ubuntu-latest
    steps:
      - name: Backup Reminder
        run: |
          echo "RECORDATORIO: Hacer backup manual de la base de datos"
          echo "Ejecuta: backup_manual.bat o backup_manual.sh"
